[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mll-from-scratch"
version = "0.1.0"
dependencies = [
    "datasets",
    "numpy",
    "tiktoken",
    "torch",
]

[tool.setuptools.packages.find]
where = ["src"]